#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../bower-files/bin/bower-files" "$@"
  ret=$?
else 
  node  "$basedir/../bower-files/bin/bower-files" "$@"
  ret=$?
fi
exit $ret
